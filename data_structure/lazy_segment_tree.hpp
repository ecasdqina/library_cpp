#pragma once

#include <vector>
#include <cstdint>

#include "../data_structure/monoid.hpp"

namespace cplib {
template<class A, class B, (A)&(A, B) effect> class lazy_segment_tree {
public:

private:
	std::vector<A> data;

public:


};
}
